import React, { Component } from 'react'
import { Typography, Input, Paper, Button } from 'material-ui'

import Search from './search'

export default class UI extends Component {
  render() {
    return <Search />
  }
}
