{
  "name": "graphql",
  "version": "1.0.0",
  "description": "",
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "current"
          }
        }
      ]
    ]
  },
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "build": "babel ./src/ -d ./dist/",
    "serve": "npm run clean && npm run build && nodemon --watch dist",
    "start": "node ./dist/",
    "dev": "nodemon dist --exec babel-node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^1.3.2",
    "axios": "^0.18.0",
    "express": "^4.16.2",
    "graphql": "^0.13.1",
    "graphql-tools": "^2.21.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.12.1"
  }
}
